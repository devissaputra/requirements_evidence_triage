[project]
name = "requirements-evidence-triage"
version = "1.1.0"
description = "Empirical requirements-classification evaluation with oracle and prediction-only human-review triage."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
